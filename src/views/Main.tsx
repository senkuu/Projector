import React from 'react';
import { Text, View } from 'react-native';
import Header from 'components/Header';

const Main: React.FC = () => {
  return (
    <View>
      <Header />
    </View>
  );
};

export default Main;
